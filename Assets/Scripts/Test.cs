using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    //public GameObject objectToInstantiate; // Reference to the prefab of the object to be instantiated
    //private Vector3 initialScale;

    //public float height;
    //public float value;

    //void innerCircleY()
    //{
    //    height = Mathf.Sqrt(3) * 2.9f / 2f;
    //}

    //void Start()
    //{
    //    innerCircleY();
    //    float newPosition = ((height / 2f)  /10 ) + 0.04f;
    //    value = (height / 2f) / 10f;
    //    // Get the size of the base object (assuming it's a cube or a similar shape)
    //    Vector3 baseObjectSize = transform.localScale;

    //    // Calculate the position for the new object on top
    //    Vector3 positionOnTop = transform.position + new Vector3(0f, newPosition, 0f);

    //    // Instantiate the object on top
    //    GameObject newInstance = Instantiate(objectToInstantiate);
    //    newInstance.transform.parent = transform;
    //    newInstance.transform.position = positionOnTop;
    //    newInstance.transform.position.y = ;
    //    // Parent the instantiated object to the base object (optional)
    //    // newInstance.transform.parent = transform;

    //}
}
